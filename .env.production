# WakeDock Production Environment Configuration

# =============================================================================
# PATHS & VOLUMES
# =============================================================================
# Base directory for all WakeDock data
WAKEDOCK_DATA_DIR=/Docker/config/wakedock

# Individual service data directories  
WAKEDOCK_CORE_DATA=/Docker/config/wakedock/wakedock-core
WAKEDOCK_LOGS_DIR=/Docker/config/wakedock/logs
WAKEDOCK_CONFIG_DIR=/Docker/config/wakedock/config

CADDY_DATA_DIR=/Docker/config/wakedock/caddy-data
CADDY_CONFIG_DIR=/Docker/config/wakedock/caddy-config

DASHBOARD_DATA_DIR=/Docker/config/wakedock/dashboard

# Development database directories (for dev environment)
POSTGRES_DATA_DIR=/Docker/config/wakedock/postgres
REDIS_DATA_DIR=/Docker/config/wakedock/redis

# =============================================================================
# NETWORK CONFIGURATION
# =============================================================================
# Network name for all WakeDock services
WAKEDOCK_NETWORK=caddy_net

# External ports
WAKEDOCK_CORE_PORT=8000
CADDY_HTTP_PORT=80
CADDY_HTTPS_PORT=443
CADDY_ADMIN_PORT=2019
DASHBOARD_PORT=3000

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# Core application settings
WAKEDOCK_LOG_LEVEL=INFO
WAKEDOCK_DEBUG=false
WAKEDOCK_RELOAD=false

# Dashboard settings
NODE_ENV=production
WAKEDOCK_API_URL=http://wakedock:8000

# =============================================================================
# DOCKER SETTINGS
# =============================================================================
# Docker socket path
DOCKER_SOCKET_PATH=/var/run/docker.sock

# Container restart policy
RESTART_POLICY=unless-stopped

# =============================================================================
# SECURITY SETTINGS
# =============================================================================
# Database credentials
POSTGRES_DB=wakedock
POSTGRES_USER=wakedock
POSTGRES_PASSWORD=secure_production_password_change_me

# Redis settings
REDIS_PASSWORD=secure_redis_password_change_me

# =============================================================================
# DOMAIN & SSL
# =============================================================================
# Your domain configuration
WAKEDOCK_DOMAIN=mtool.ovh
WAKEDOCK_SUBDOMAIN_PREFIX=

# SSL/TLS settings
CADDY_AUTO_HTTPS=true
CADDY_ADMIN_API_ENABLED=false
